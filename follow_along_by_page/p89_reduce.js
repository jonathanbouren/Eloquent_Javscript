
// function reduce(array, combine, start) {
//   let current = start;
//   for (let element of array) {
//     current = combine(current, element);
//   }
//   return current;
// }

// console.log(reduce([1, 2, 3, 4, 5], (a, b) => a * b, 1))


console.log(['jon', 'sasha', 'taco'].reduce((a, b) => a += (' ' + b)))